numpy>=1.26.0
scipy>=1.14.0
qutip>=5.0.0
torch>=2.4.0
matplotlib>=3.9.0
seaborn>=0.13.0
networkx>=3.3
pyyaml>=6.0
tqdm>=4.66.0
wandb>=0.21.0
# weave>=0.25.0   # optional: for W&B Weave traces (Grok prompts/responses, parse success)
optuna>=4.0.0
optuna-dashboard>=0.15.0  # optional: web UI for trials
pytest>=8.0.0
streamlit>=1.28.0  # Green Horizon / geophysical demo app
plotly>=5.18.0    # interactive 3D surface/scatter/mesh, WebGL; Streamlit, W&B, Green Horizon
kaleido>=0.2.0    # static export for Plotly (e.g. W&B images)
pandas>=2.0.0     # dataframes for charts and logging
fastapi>=0.110.0  # GQ Live Brain API server
uvicorn>=0.30.0   # ASGI server for FastAPI
python-multipart>=0.0.9  # AutoQMS API file uploads
requests>=2.31.0  # Streamlit -> FastAPI communication
python-dotenv>=1.0.0  # load XAI_API_KEY from repo-root .env for API + training
websockets>=12.0  # AISStream.io real-time AIS (GET /data/ais)
pennylane>=0.40.0
pennylane-qiskit>=0.42.0  # qiskit.remote + IBM Quantum (see src/quantum_circuit_optimizer.py)
pennylane-lightning>=0.41.0  # GPU sim: set QM2ARL_USE_LIGHTNING=1 (see docs/QUANTUM_STACK.md)
bluequbit>=0.18.0  # BlueQubit cloud quantum: set BLUEQUBIT_API_TOKEN (see docs/QUANTUM_STACK.md)
Brian2>=2.5.1  # neuromorphic SNN prototyping (see src/neuromorphic_network.py)
numba>=0.59.0  # optional: JIT-compile discounted-returns hot loop (see src/efficient_models.py); pure-numpy fallback if absent
